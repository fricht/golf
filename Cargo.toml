[package]
name = "Golf"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 3
debug = false
overflow-checks = false
panic = "abort"

[profile.release]
opt-level = 3
debug = false
overflow-checks = false
panic = "abort"

[dependencies]
embedded-alloc = "0.6.0"
